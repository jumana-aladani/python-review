# Review: find_max

## الغرض المفترض
دالة `find_max` مفترض أنها تُرجع أكبر رقم موجود في القائمة المُمرَّرة إليها، بحسب اسمها وتوثيقها (docstring).

## التتبّع اليدوي بالمدخل [3, 1, 9]

- `biggest = numbers[0]` → `biggest = 3`
- الحلقة `for i in range(len(numbers) - 1)` تعطي `range(2)`، أي `i = 0, 1` فقط (المؤشر `2` لا يُزار).
- `i=0`: `numbers[0]=3 > biggest=3`؟ لا → `biggest` يبقى `3`
- `i=1`: `numbers[1]=1 > biggest=3`؟ لا → `biggest` يبقى `3`
- `return biggest` → الناتج الفعلي هو `3`

## الخطأ (bug)
رغم أن الاسم والـ docstring يدّعيان أن الدالة تُرجع أكبر رقم في القائمة، إلا أنها لا تفعل ذلك فعلياً. يوجد **bug** واضح من نوع off-by-one في السطر `range(len(numbers) - 1)`، حيث يجعل الحلقة تتوقف قبل الوصول إلى آخر مؤشر في القائمة، فيتم تجاهل العنصر الأخير (`9`) تماماً من المقارنة رغم أنه أكبر رقم فعلياً في المدخل. هذا الـ bug يجعل الدالة تُرجع نتيجة خاطئة (`3` بدلاً من `9`) في أي حالة يكون فيها أكبر رقم هو آخر عنصر في القائمة.
